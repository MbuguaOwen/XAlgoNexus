diff --git a/.github/workflows/auto-monitoring-sync.yml b/.github/workflows/auto-monitoring-sync.yml
new file mode 100644
index 0000000..aaaaaaa
--- /dev/null
+++ b/.github/workflows/auto-monitoring-sync.yml
@@
+name: Auto Monitoring Config Sync
+
+on:
+  workflow_dispatch:
+  push:
+    paths:
+      - prometheus.yml
+      - alert.rules.yml
+      - alertmanager.yml
+      - grafana/**
+
+jobs:
+  sync-monitoring-stack:
+    runs-on: ubuntu-latest
+    permissions:
+      contents: write
+    steps:
+      - name: Checkout repo
+        uses: actions/checkout@v4
+
+      - name: Commit and push monitoring config
+        run: |
+          git config user.name "XAlgo Sync Bot"
+          git config user.email "xalgo-bot@example.com"
+          git add prometheus.yml alert.rules.yml alertmanager.yml
+          git add grafana/provisioning/datasources/* grafana/provisioning/dashboards/*
+          git commit -m "🔧 Sync Prometheus & Grafana monitoring stack [bot]" || echo "Nothing to commit"
+          git push
+
+      - name: Tag release
+        run: |
+          VERSION_TAG="monitoring-v1"
+          git tag $VERSION_TAG || echo "Tag $VERSION_TAG already exists"
+          git push origin $VERSION_TAG || echo "Tag push skipped (already exists)"
